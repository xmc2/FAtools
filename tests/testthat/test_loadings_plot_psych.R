context("this is the test purpose")

library(datasets)
library(psych)
library(testthat)
# corr.matrix <- cor(mtcars)
# results <- psych::fa(corr.matrix, 3, rotate = "varimax")
# FAtools::loadings_table_psych(results, cutoff = 0.3, roundto = 2, fac.lab = c("Factor 1", "Factor 2", "Factor 3"))


test_that("test_label", {
        # TO DO: replace this fake test with real tests..
        expect_equal(
                2,2
                )
})

